<Window x:Class="DirectoryMonitor.Views.ActionEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Action Editor" Height="450" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Name -->
        <Label Grid.Row="0" Content="Name:" FontWeight="Bold"/>
        <TextBox x:Name="NameBox" Grid.Row="1" Margin="0,0,0,10"/>

        <!-- Action Type -->
        <Label Grid.Row="2" Content="Action Type:" FontWeight="Bold"/>
        <ComboBox x:Name="ActionTypeCombo" Grid.Row="3" Margin="0,0,0,10" 
                  SelectionChanged="ActionTypeCombo_SelectionChanged">
            <ComboBoxItem Content="Notification"/>
            <ComboBoxItem Content="Run Program"/>
            <ComboBoxItem Content="Copy File"/>
        </ComboBox>

        <!-- Parameters Panel -->
        <StackPanel x:Name="ParametersPanel" Grid.Row="4" Margin="0,0,0,10"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="OkButton" Content="OK" Width="80" Margin="0,0,5,0" Click="OkButton_Click"/>
            <Button x:Name="CancelButton" Content="Cancel" Width="80" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>